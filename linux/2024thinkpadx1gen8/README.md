# Thinkpad X1 configs

`.config` and `home` are at the same level, don't be fooled. 

There's a setup script for the ipu6 webcam.

For the webcam, firefox also needs these in `about:config` :

```
media.webrtc.camera.allow-pipewire = true
media.webrtc.capture.allow-pipewire = true
```
